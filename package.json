{
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0"
  },
  "scripts": {
    "test": "jest",
    "watch": "jest --watchAll",
    "build": "docker build . -t perceptioncontrol/sorter",
    "pull": "docker pull perceptioncontrol/sorter",
    "example": "node ./src/generator.js; node ./src/sorter.js"
  }
}
